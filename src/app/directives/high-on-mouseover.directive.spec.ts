/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HighOnMouseoverDirective } from './high-on-mouseover.directive';

describe('HighOnMouseoverDirective', () => {
  it('should create an instance', () => {
    let directive = new HighOnMouseoverDirective();
    expect(directive).toBeTruthy();
  });
});
