/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BruttoPipe } from './brutto.pipe';

describe('BruttoPipe', () => {
  it('create an instance', () => {
    let pipe = new BruttoPipe();
    expect(pipe).toBeTruthy();
  });
});
